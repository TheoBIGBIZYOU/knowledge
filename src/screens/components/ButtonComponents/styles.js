import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    buttonComponents: {
        width: '100%',
        backgroundColor: 'white',
        borderRadius: 10,
        alignItems: 'center',
        padding: 10
    }
})