import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    receiverMessage: {
        alignSelf: 'flex-start',
        color: '#000',
        backgroundColor: '#fff',
        padding: 10,
        marginBottom: 5,
        marginLeft: 10,
        borderRadius: 10,
        borderWidth: 1,
        borderColor: '#fff',
        overflow: 'hidden'
    }
})